[Service]
# The registry daemon can escape this service's cgroup under UWSM. Remove it
# when the accessibility bus stops so compositor changes do not accumulate one.
ExecStopPost=-/usr/bin/pkill -f ^/usr/lib/at-spi2-registryd
