[Service]
# Boot uses FONT=ter-v16n in vconsole.conf. Only the greeter VT needs a
# larger font; 32px made both the boot log and tuigreet too large.
ExecStartPre=/usr/bin/setfont ter-v24n -C /dev/tty1
